# E802 fail: conditional edge has string 'to' — silently becomes normal edge
state:
  response: str
  score: float

nodes:
  generate:
    type: llm
    prompt: generate
    state_key: response
  critique:
    type: llm
    prompt: critique
    state_key: score

edges:
  - from: START
    to: generate
  - from: generate
    to: critique
  - from: critique
    to: generate
    type: conditional
    condition: "score < 0.8"

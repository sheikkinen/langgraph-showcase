# W007 pass: variable expressions use correct {state.field} syntax
prompts_relative: true
prompts_dir: prompts
state:
  name: str
  response: str

nodes:
  greet:
    type: llm
    prompt: greet
    state_key: response
    variables:
      name: "{state.name}"

edges:
  - from: START
    to: greet
  - from: greet
    to: END

# W007 fail: variable expression missing state. prefix — treated as literal
prompts_relative: true
prompts_dir: prompts
state:
  name: str
  response: str

nodes:
  greet:
    type: llm
    prompt: greet
    state_key: response
    variables:
      name: "{name}"

edges:
  - from: START
    to: greet
  - from: greet
    to: END

# E006 fail: edge 'from' has a typo — 'genrate' doesn't exist
prompts_relative: true
prompts_dir: prompts
state:
  response: str

nodes:
  generate:
    type: llm
    prompt: generate
    state_key: response

edges:
  - from: START
    to: generate
  - from: genrate
    to: END

# E006 pass: all edge from/to targets reference real nodes or START/END
prompts_relative: true
prompts_dir: prompts
state:
  response: str

nodes:
  generate:
    type: llm
    prompt: generate
    state_key: response

edges:
  - from: START
    to: generate
  - from: generate
    to: END

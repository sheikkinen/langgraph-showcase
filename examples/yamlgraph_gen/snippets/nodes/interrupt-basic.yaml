# Interrupt Basic Node Snippet
# Human-in-the-loop pause point
#
# ASSEMBLY INSTRUCTIONS:
# - Interrupt node pauses execution for human input
# - Requires checkpointer (add from scaffolds/checkpointer-memory.yaml)
# - DO NOT add extra validation nodes before/after interrupt
# - Keep resume_key field exactly as defined

description: Interrupt node for user input

node:
  type: interrupt
  prompt: prompts/__NODE_NAME__
  resume_key: "{user_input_key}"
